<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Learn EJS</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <h3>Params a = <%= a %></h3>
        <% arrPerson.forEach(person => { %>
            <h3>Ten: <%= person.name %></h3>
            <p>Tuoi: <%= person.age %></p>
        <% }); %>
    </body>
    <script src="reload/reload.js"></script>
</html>